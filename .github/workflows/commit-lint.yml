name: Lint commits

on:
  push:
    branches:
      - v18
  pull_request:
    branches:
      - v18

jobs:
  lint-commits:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: wagoid/commitlint-github-action@v5
